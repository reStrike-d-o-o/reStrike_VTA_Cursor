# Repository instructions (Copilot)
Use `AGENTS.md` as the canonical rulebook.
When producing docs, prefer artifacts in `docs/api/*`, `target/doc/*`, and `docs/diagrams/*`.
For code edits, keep PRs small and include tests.
